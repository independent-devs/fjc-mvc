%li{id: dom_id(variant)}
  = form_for(variant, url: admin_product_stock_path(@product, variant), html: {method: :put}, data: {controller: "autosave admin--products--stocks--item", modify_url: modify_admin_product_stock_path(@product, variant)}) do |f|
    .grid.grid-cols-5
      .py-4.px-5
        - if f.object.is_master
          = render TagComponent.new do 
            Master
        - else
          = f.object.grouped_name
      .py-4.px-5
        - if f.object.is_master
          = render TagComponent.new do
            Master
        - else
          = f.text_field :sku, id: dom_id(variant, "sku"), class: "input-table", data: {"action": "change->autosave#save"}
      .py-4.px-5
        - if f.object.is_master
          = render TagComponent.new do
            Master
        - else
          .space-y-1
            .flex.gap-1.items-center
              = f.check_box :trackable, id: dom_id(variant, "trackable"), data: {action: 'autosave#save'}, class: "checkbox-primary"
              = f.label :trackable
            .flex.gap-1.items-center
              = f.check_box :backorderable, id: dom_id(variant, "backorderable"), data: {action: 'autosave#save'}, class: "checkbox-primary"
              = f.label :backorderable
      = render "item_count_on_hand", form: f
      = render "modify_stock", variant:
