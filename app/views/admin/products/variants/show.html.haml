.margin-max-screen.py-7.flex.flex-col.gap-3
  = render "admin/products/tabs"
  .flex.flex-col.pt-1
    %h1.text-2xl.font-semibold= t("variants.update_product_variant")
    %span.text-gray-500
      = @product.name
  = render 'form', path: admin_product_variant_path, method: :put

