- parent = @category.parent
- ancestors = parent.ancestors

.margin-max-screen.h-full.py-8.flex.flex-col.gap-5
  %h1.text-2xl.font-semibold Edit category
  .flex.flex-col.gap-1
    %label.font-semibold
      Ancestors
    .flex.gap-2.items-center
      - ancestors.each do |an|
        %div.flex.items-center.gap-2
          %span= an.name
          = icon("fa-solid", "angle-right")
      %div
        %span.text-primary.font-medium= parent.name
  = render 'form', parent:, path: admin_category_path(@category)
