= render "seo" 

.w-full.margin-max-screen.flex.flex-col.gap-7.py-7
  = render SectionComponent.new label: t("categories.section_label"), title: t("categories.section_title") do
    sample
  = render SectionComponent.new label: t("products.section_label"), title: t("products.section_title"), action_link: products_path, action_text: "All products" do         
    sample
