import { Controller } from "@hotwired/stimulus"

// Connects to data-controller="product"
export default class extends Controller {
  static targets = ["has-variant-checkbox"]

  connect() {
    console.log(this.hasVariantCheckbox)
  }
}
