# frozen_string_literal: true

module ImagesHelper
  def image_variant_selection(product)
    variants = product.variants.non_master_variants.not_deleted.sort_by_position

    [['All', nil]]
  end
end
